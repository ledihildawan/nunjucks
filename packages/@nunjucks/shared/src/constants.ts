// Placeholder pattern for synthesised error definitions that are never
// matched against a real message. Shared across packages so the literal is
// defined once.
const MATCH_ANY_RE = /./;

export { MATCH_ANY_RE };
