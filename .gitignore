node_modules
coverage
coverage.txt
.#*
docs/_site
docs/files
.bun
dist
build
*.log
.nyc_output
/nunjucks-original

# local tooling state (opencode plans/session)
.opencode/

# local environment overrides (never commit secrets)
.env*

# ephemeral cognitive scratchpad (audit/verification artifacts)
/herment/

# ephemeral agent scratchpad (prototype/verification zone)
/.sandbox/

# local scratch space
/tmp/

# local agent settings
.claude/settings.local.json
